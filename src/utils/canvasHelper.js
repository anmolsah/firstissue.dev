/**
 * This utility was used to workaround html2canvas limitations with oklch colors.
 * We now use html-to-image which natively supports modern CSS features.
 */
export const fixOklchInElement = () => {};
